import React from 'react';
import AdminDashboard from '../components/Admin/AdminDashboard';

const AdminPanel: React.FC = () => {
  return <AdminDashboard onPageChange={() => {}} />;
};

export default AdminPanel;
